orakelqueue.app.jwtSecret=${JWT_SECRET}

spring.profiles.active=dev
#spring.profiles.active=prod
#spring.profiles.active=test

# PostgreSQL
spring.datasource.platform=postgres
spring.datasource.url=${ORAKEL_DATABASE_URL}
spring.datasource.username=${ORAKEL_DATABASE_USERNAME}
spring.datasource.password=${ORAKEL_DATABASE_PASSWORD}

#drop and create table again, good for testing, comment out in production
spring.jpa.hibernate.ddl-auto=create-drop